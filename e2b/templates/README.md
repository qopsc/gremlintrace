# E2B sandbox templates

Template definitions (`base`, `kodus-sandbox`, `kodus-sandbox-graph`) built with the E2B SDK v2 against the local API.

**Task 4** adds `kodus-template.ts`, `build-templates.ts`, and unit tests.
