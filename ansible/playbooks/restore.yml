---
# Implemented in M2: restore from a qops backup bundle.
- name: Codereviewer restore
  hosts: codereviewer
  become: true
  vars_files:
    - ../../versions.yml
    - ../../group_vars/all.yml
  tasks:
    - name: Restore placeholder
      ansible.builtin.debug:
        msg: "restore.yml is implemented in M2"
      tags:
        - restore
