---
# Implemented in Task 11: remove services, nftables, E2B runtime state, and Kodus compose stack.
- name: Codereviewer uninstall
  hosts: codereviewer
  become: true
  vars_files:
    - ../../versions.yml
  pre_tasks:
    - name: Load documented defaults without overriding inventory
      ansible.builtin.include_tasks: ../tasks/load-defaults.yml
  tasks:
    - name: Uninstall placeholder
      ansible.builtin.debug:
        msg: "uninstall.yml is implemented in Task 11"
      tags:
        - uninstall
