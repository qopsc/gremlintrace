# CI workflows

GitHub Actions workflows for building the E2B dist tarball and running the installer matrix.

| Workflow | Task |
|---|---|
| `build-e2b.yml` | Task 3 |
| `installer-matrix.yml` | Task 12 |
| `restore-drill.yml` | M2 |

This directory starts empty until those tasks land.
